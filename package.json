{
  "name": "w1c-workspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "dev:docs": "pnpm --filter docs dev",
    "dev:storybook": "pnpm --filter storybook dev",
    "dev:lib": "pnpm --filter @w1c/lib dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  }
}
